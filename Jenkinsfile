pipeline {
    agent {label "sys-admin-mnf"}

    stages {
        stage('Build') {
            steps {
                echo 'build'
                sh "ls"
            }
        }
        stage('Test') {
            steps {
                echo 'test'
                sh "ls"
            }
        }
        stage('Quality') {
            steps {
                echo 'quality'
                sh "ls"
            }
        }
        stage('Deploy') {
            steps {
                echo 'deploy'
                sh "ls"
            }
        }
    }
}
